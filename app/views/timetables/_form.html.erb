<%= form_for([@employee, @timetable]) do |f| %>
  <% if @timetable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@timetable.errors.count, "error") %> prohibited this timetable from being saved:</h2>

      <ul>
      <% @timetable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.label @timetable.date %>
  </div>
  <div class="field">
    <%= f.label :slot1 %><br>
    <%= f.check_box :slot1 %>
  </div>
  <div class="field">
    <%= f.label :slot2 %><br>
    <%= f.check_box :slot2 %>
  </div>
  <div class="field">
    <%= f.label :slot3 %><br>
    <%= f.check_box :slot3 %>
  </div>
  <div class="field">
    <%= f.label :slot4 %><br>
    <%= f.check_box :slot4 %>
  </div>
  <div class="field">
    <%= f.label :employee_id %><br>
    <%= f.label @timetable.employee_id %>
  </div>
  <div class="actions">
    <%= f.submit "Save"%>
  </div>
<% end %>
